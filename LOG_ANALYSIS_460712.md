# 对局日志 `game_460712.log` 复盘（2026-08-16）

## 0. 先确认身份：我们是 `player2 / player284`，这一局我们**赢了**

日志头是 `{"player1":"V3Vision7","player2":"player284"}`。谁是我们？三条证据：

1. **视野**：日志里 `grid` 全程是 5x5 视野的雾化视图，且雾的中心永远跟着
   player2 的两个角色；player1 的 `units[].position` 有 343 回合是 `null`，
   143 回合出现 1 个、14 回合出现 2 个——这正是"进入我方视野才可见"的表现。
   即日志是**从 player2 视角**渲染的。
2. **时延**：`cost` 不是累计值，是**每回合决策耗时（纳秒）**。

   | | median | P90 | P99 | max | 全局合计 |
   |---|---:|---:|---:|---:|---:|
   | player1 | 2.44 ms | 3.00 ms | 4.39 ms | 46.3 ms | 1289 ms |
   | **player2** | **19.4 us** | **22.0 us** | **27.2 us** | **29.2 us** | **9.8 ms** |

   player2 的量级正是 `strategy.cpp`；player1 是 Python 量级。
3. **回放验证（决定性）**：把日志里 player2 每回合的 `GameInput` 抽出来喂给当前
   `player.so`，第 1–7 回合的 `k` 和 6 个 action **逐字节完全一致**。

   ```text
   r1 actual k=5 a=[1,2,1,2,1,0] | ours k=5 a=[1,2,1,2,1,0]
   r2 actual k=5 a=[2,1,2,1,2,3] | ours k=5 a=[2,1,2,1,2,3]
   ...
   ```

   （回放里 `snapshot_valid` 只能置 0，所以往后信念会发散，全局吻合率降到
   187/500；前 7 回合的逐字吻合已足够定身份。）

**结论：我们是 player2，终局 2262 : 1908，赢 354。** 所以这局不是"输了"，
但**赢得远不够多**：全场 500 轮理论产出约 9350 金币，我们只拿到 24%。

> 顺带一提：`round 0` 我们输出的 `a=222333`，日志记成 `a=224334`——第 3 步和第 6 步
> 撞上了 `[0,13]` / `[16,3]` 的障碍物，被判非法、静默浪费。这是势场用曼哈顿距离、
> 不感知障碍的直接后果（README 五附 B 已指出）。

---

## 1. 核心诊断：**六步规划器不是瓶颈，它已经接近最优**

对每一回合，用日志里我们当时**实际能看到的** grid，做一次精确 DFS，
枚举全部 `k` 分配，求"6 步内最多能捡多少金币"，再和实际拾取量对比：

```text
全局可见最优:  2483 金币
实际拾取:      2400 金币
捕获率:        96.7%
全局漏掉:      83 金币（500 轮合计）
```

而且这 83 里还有相当一部分是**正确的放弃**。例：

**round 156** —— 我们看到 `(8,11)` 有 16 金币，却一步没动（两个角色都只输出了
无效动作 + STAY）：

```text
      col 9  10  11  12
row 7    -1   0  -3   0     <- (7,11) 炸弹
row 8    -1   0  16  -1     <- 金币被 (8,10)/(8,12) 两个障碍夹住
row 9    -1   0  -3   0     <- (9,11) 炸弹
```

唯一的两条入口 `(7,11)` `(9,11)` 都是炸弹。当时两个角色分别持有 419 / 326 金币，
踩雷代价 `ceil(0.1*419)=42` 和 `ceil(0.1*326)=33`，都远大于 16。**不去是对的。**

> 换句话说，**扣掉炸弹后的真实捕获率接近 99%**。当前 beam（width 3）在
> "本回合六步之内"这个子问题上几乎没有留下钱。

这条结论直接改写了 README 五附 A 的判断："好路线在 width 3 被剪掉"这个
失效模式**在真实对局里几乎没有发生**。

---

## 2. 真正的瓶颈：每一步的**边际产出**太低

| 指标 | 实测 |
|---|---:|
| 有效移动步数（500 轮 × 6） | 2901 / 3000（99 步是 STAY 或非法） |
| 其中**落在有金币格**上的步数 | **618 = 21.3%** |
| 落上去时该格平均金币 | 7.36 |
| **金币 / 有效步** | **0.827** |
| 拾取为 0 的回合 | **194 / 500 = 38.8%** |
| 视野内**一枚金币都没有**的回合 | **109 / 500 = 21.8%** |
| 回合开始时我方视野内可见金币总量（均值） | **8.7** |

**78.7% 的步数是纯位移。** 对照 README §2.2 的理论：一个纪律严明的 12 轮中心
巡逻圈，每格积累 `0.1668×12 ≈ 2.0` 金币，`ceil(0.65×2)=2` 整堆拿走，
即 **2.0 金币/步 ≈ 12 金币/轮**。我们是 0.827 金币/步、4.80 金币/轮。

**天花板在 2.4x，而且全部在跨回合的路由与目标选择上，不在回合内的搜索上。**

### 2.1 重访间隔是双峰的

对中心区（区域 1）统计"同一格两次被我方踩到"的间隔：

```text
n=361   median 12 轮   P90 148 轮
```

中位数 12 恰好命中理论最优周期——说明**核心巡逻区确实在按理论节奏跑**。
但 P90 = 148 说明另有一批中心格被长期遗弃。产出被 NPC 收走。

### 2.2 一个角色长期挂机 / 蹲角落

```text
unit0: 166/500 轮分到 0 步，最长连续 17 轮不动；去过 152 个不同格
unit1: 145/500 轮分到 0 步，最长连续  8 轮不动；去过 131 个不同格
       其中 (2,16) 一格待了 66 轮 —— 地图角落，区域 5
k 分配: (0,6) 166 次 + (6,0) 145 次 = 62% 的回合，一个角色完全不动
两角色平均距离 12.2 格
```

`(0,6)` / `(6,0)` 本身不是错的——beam 认为另一角色的边际收益为 0。
但连续 17 轮不动、一格蹲 66 轮，意味着**势场在那个角色周围长期是平的**，
它既没有金币可拿，也没有被驱赶去做任何有价值的事（探索、扫雷、堵路）。
这是"边际曲线选 k"的结构性副作用：**它只会问"这一步值不值"，
不会问"这个角色这 20 轮该待在哪个区"。**

### 2.3 贫富分工从未真正发生

README §2.6 的核心洞察是：金币按角色分开记（`my_units_gold[2]`），胜负看总和，
所以应当刻意让一个角色保持贫穷去踩雷探路。实测：

```text
r49   156 / 71
r149  408 / 314
r249  704 / 458
r349  832 / 684
r449 1153 / 912
r499 1257 / 1005     <- 比值仅 1.25
```

**两个角色一起变富。** 后果直接可见：

- 全局炸弹/踩踏损失 138 金币，其中两次大额都砸在富角色身上：
  - `round 254`：711 → 641，一次踩雷 **-72**
  - `round 119`：267 → 216，**一回合连踩两颗雷** -27 -24 = **-51**
- `round 156` 那 16 金币，如果有一个角色一直保持在 ~50 金币，
  踩雷代价只有 5，净赚 11 **并且顺手清掉一颗雷**。

`classify_roles` 里的 `SCOUT_CLEAR_SHARE` / `SCOUT_INFO_BONUS` 只在
`phase < 5`（每 20 轮里的前 5 轮）且 `round < 25` 时有强度，
其余时间 `info_strength = 0`、`clear_strength = 0`，
角色效用退化成 0 —— **分工机制 500 轮里绝大部分时间是关着的。**

### 2.4 视野购买是死代码

```text
两方 vision_spent 全程 = 0；vision_r 全程 = {"2": 2}
```

`vision()` 的门槛：`budget = posture × 0.15 × income_`，要买 9x9 需要
`income_ ≥ 20`（posture=1 时），买 7x7 需要 `income_ ≥ 13.3`；
兜底分支要求 `income_ ≥ BLIND_BONUS = 10`。

我们的实际收入 EMA 稳定在 **4.80 金币/轮**。**这三个门槛一个都够不到，
所以 500 轮里一次都没买过视野。** 与此同时：

- 21.8% 的回合视野里一枚金币都没有；
- 我们对"看得见且走得到"的金币捕获率是 96.7%。

**信息就是瓶颈，而我们攒了 2262 金币一分钱没花在信息上。**
（`vp` 费用对局结束后结算，7x7 = 2 金币/轮、9x9 = 3 金币/轮。）

### 2.5 终局在丢分

按 50 轮分段的净增（P1 / P2）：

```text
r0-49    181 / 227      r250-299   145 /  95   <- r254 踩雷 -72
r50-99   210 / 223      r300-349   205 / 259
r100-149 125 / 272      r350-399   102 / 209
r150-199 212 / 250      r400-449   238 / 340
r200-249 185 / 190      r450-499   305 / 197   <- 领先从 462 缩到 354
```

最后 50 轮我们被反超 108 金币。细看 `r450-459`：P1 +96 / P2 +24。
`ENDGAME_ROUNDS = 15` 的退火（`pot_weight → 0`、`risk_multiplier → 2x`）
只覆盖最后 15 轮，但 `r450-459` 已经开始掉——更可能的原因是
终局金币堆积得更大（对手 P1 在 r480-489 单段 +92），
而我们的 `MAX_EST = 60` 和 `AGE_CAP = 14` 让远处大堆的估值被削平，
势场拉不动我们过去。

---

## 3. 结论与优先级

**按实测边际收益重排 README 第五章的路线图：**

| 优先级 | 改动 | 依据 | 预估 |
|---|---|---|---|
| **P0** | **修好视野购买**（把门槛从"收入 EMA 绝对值"改成 VOI/相对预算） | §2.4，21.8% 回合完全看不到金币，2262 金币零投资 | 大 |
| **P0** | **跨回合的区域分配 / 巡逻承诺**（给每个角色一个 12 轮周期的责任区，而不是每回合重选 k） | §2.1 P90=148、§2.2 一格蹲 66 轮 | 大 |
| **P1** | **让贫富分工全程生效**，不要只在每周期前 5 轮开 | §2.3，双大额踩雷 + round 156 类型的机会 | 中 |
| **P1** | **势场改成障碍感知**（深度 ≤6 的 BFS，或至少对 `pot_` 做障碍屏蔽） | round 0 就浪费 2/6 步 | 中 |
| **P2** | 终局估值不要过早退火 / 提高 `MAX_EST` | §2.5，最后 50 轮丢 108 | 小-中 |
| **P3** | 精确 DFS 取代 beam（本次已做） | §1，全局上限 ~83 金币 | 小（+3~4%） |

**这份日志最重要的一句话：不要再调回合内的搜索了。**
它已经交付了可见信息的 96.7%。剩下的钱全在"看不见"和"没去对地方"这两件事上。
